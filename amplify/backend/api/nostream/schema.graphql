type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  lastName: String!
  email: String!
  description: String
}

type ContactUs @model {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  description: String!
}
 